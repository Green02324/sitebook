// Display name for non-job costs, used in generated PDFs and in API responses
// that feed straight into the UI (ledger rows, storage breakdown). Routes and
// table names still use "overhead" — this is presentation only.
export const OVERHEAD_LABEL = "Operating Expenses";
